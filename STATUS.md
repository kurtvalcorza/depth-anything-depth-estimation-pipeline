# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package with its adaptation contract (`evaluate`, `adapt` with the zero-shot / frozen / unfrozen policies, `save_artifact` / `from_artifact`; `predict` unchanged; the pinned DIODE view table and BYOD loaders in `samples.py`; aligned AbsRel, δ1 and the two priors in `metrics.py`), offline unit tests (32, no weights required, plus 5 notebook-parity tests and 2 model-backed tests that run when the snapshot is staged), a local CPU execution of the committed notebook blob in the Windows venv (priors 0.335 / 0.260 → zero-shot 0.180 → selected policy `unfrozen last 2 blocks + DPT neck and head` 0.167 AbsRel on 8 held-out views; recorded in `docs/release-verification.md`), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial notebook `tutorials/depth_anything_depth_estimation_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The earlier `TASK-INFERENCE` notebook's Kaggle Tesla T4 run (blob `3ed3ea0`, commit `a1b81b4`; `evidence/release-verification-kaggle.json`) is retained as history and is not evidence for the `E2E` blob, so the repository steps back from **Release-grade** to **Candidate** until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
